[ numbers.imp - liczby ]
DECLARE
	tab(0:2); a; b; c;
IN
	tab(0) := 8;
	tab(1) := 2;
	tab(2) := tab(0) / tab(1);
	
	WRITE tab(0);
	WRITE tab(1);
	WRITE tab(2);
	
	b:=8;
	a:=2;
	c:=b/a;
	
	WRITE b;
	WRITE a;
	WRITE c;
END
